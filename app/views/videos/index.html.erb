<header>
  <%= link_to "Quizknock DB",root_path, class:"header-logo" %>
</header>
<div class="container">
  <div class="container-header">
    <h1>動画一覧</h1>
    <%= link_to "+新規追加", new_video_path, class:"new-button" %>
  </div>
  <div class="index-table-wrapper">
    <div class="table-head">
      <span class="id-column">ID</span>
      <span class="first-column">名前</span>
      <span class="second-column">アップロード日</span>
      <span class="third-column">アップロード日</span>
    </div>
    <ul class="table-body">
      <% @videos.each do |video| %>
        <li>
          <div class="item-data">
            <span class="id-column"><%= video.id %></span>
            <span class="first-column"><%= link_to video.name, video.url %></span>
            <span class="second-column"><%= video.upload_at %></span>
            <div class="item-menu">
              <%= link_to "編集", edit_video_path(video.id) %>
              <%= link_to "削除", video_path(video.id),
                method: :delete, data: {confirm: "本当に削除しますか？"} %>
            </div>
          </div>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
